# Create a browser key in MapTiler Cloud and restrict it to your production domain.
VITE_MAPTILER_KEY=

# Leave empty when the API is served from the same domain (recommended).
# For a separate API domain, use a full origin such as https://api.example.com.
VITE_API_BASE_URL=
